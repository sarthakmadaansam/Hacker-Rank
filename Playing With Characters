#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#
int main() 
{

  char ch[10],s[100],buf[1000];
   
 
  fgets(ch,10,stdin);
  printf("%s",ch);
fgets(s, 100, stdin);
printf("%s",s);
fgets(buf, 1000, stdin); 
printf("%s", buf);
return 0;
}
